#ifndef GAME_H
#define GAME_H

#include "GameState.h"
//#include <SFML/Graphics.hpp>
/*
class Game
{
	public :
		void setState(GameState* state);
		int afficherInterface(sf::RenderWindow& window, sf::Font monopolyFont, int nbPlayer);
		void jouerMusique();
		static Game& getInstance() {
			static Game instance;
			return instance;
		}

	private :
		Game() {} // Constructeur privé pour empêcher l'instanciation directe
		Game(const Game&) = delete; // Supprime le constructeur de copie
		Game& operator=(const Game&) = delete; // Supprime l'opérateur d'affectation
		GameState* currentState_;
};
*/
#endif

